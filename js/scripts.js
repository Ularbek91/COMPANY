$(document).ready(function () {
    let position = 0;
    const slidesToshoW = 1;
    const slidesToScrol = 1;
    const slides = $(',slider');
    const block = $(',slider_block');
    const item = $(',slider_item');
    const btnPrev = $(',slider_button_next');
    const btnNext = $(',slider_button_prev');
    const itemWidth = slider.width() / slidesToshoW;

});
slider
slider_block
slider_item